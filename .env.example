# Firebase Configuration - Copy this to .env and fill in your actual values
VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# Note: These are frontend environment variables and will be exposed to the client
# Firebase client SDK keys are safe to expose as they're designed for client-side use
# Security is enforced through Firebase Security Rules, not by hiding these keys